// Font definition for pdfmake with Arabic support
this.pdfMake = this.pdfMake || {};
this.pdfMake.vfs = {
  // Include base64 encoded fonts here
  // This is a placeholder - we'll use the default fonts from pdfmake
};
